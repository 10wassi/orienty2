export interface FAQCardProps {
  title: string;
  description?: string;
  isExpanded?: boolean;
  iconSrc: string;
}

export interface FAQSectionProps {
  title: string;
  subtitle: string;
  description: string;
}
