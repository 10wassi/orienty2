export interface StepProps {
  number: number;
  description: string;
}

export interface SocialButtonProps {
  icon: string;
  altText: string;
  label: string;
}

export interface RawImgProps {
  image: string;
  altText: string;
  className?: string;
}
